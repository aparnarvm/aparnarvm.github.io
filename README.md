# Aparna RVM - GitHub Pages

This repository is configured as a Jekyll-based GitHub Pages site.

## 🚀 Quick Start

Visit your live site at: **https://aparnarvm.github.io**

### Setup Instructions

1. **Rename this repository** to `aparnarvm.github.io` in GitHub Settings
2. **Enable GitHub Pages** (Settings → Pages → Deploy from main branch)
3. Your site will be live in a few minutes!

### Local Development

```bash
bundle install
bundle exec jekyll serve
```

Visit `http://localhost:4000` to preview.

## 📁 File Structure

- `index.md` - Your homepage with all profile content
- `_config.yml` - Jekyll configuration (customize title, description, theme, etc.)
- `Gemfile` - Ruby dependencies for Jekyll

## ✏️ Customizing Your Site

- **Change theme**: Edit the `theme:` line in `_config.yml`
- **Add pages**: Create new `.md` files (about.md, projects.md, etc.)
- **Edit homepage**: Modify `index.md`

For detailed setup instructions, see [GITHUB_PAGES_SETUP.md](GITHUB_PAGES_SETUP.md)

---

**Current Profile Content**: All your bio, research focus, technical stack, and contact information is in `index.md`
